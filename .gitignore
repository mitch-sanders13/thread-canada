# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Env / secrets — never commit credentials
.env
.env.*
!.env.example

# Shopify CLI / local tooling
.shopify/
.shopifyignore

# Node (if theme tooling is added later)
node_modules/
npm-debug.log*
yarn-error.log*

# Theme zip exports (source files are tracked instead)
*.zip
